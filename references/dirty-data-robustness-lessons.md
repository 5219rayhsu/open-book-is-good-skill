# 髒資料韌性教訓

方法正本在 `dirty-data-robustness.md`。本檔收錄踩坑紀錄，節號沿用 dirty-data-robustness.md 原編號、永不重編，所以舊引用只要把檔名改成 `dirty-data-robustness-lessons.md` 就能解析。

## 十二、格式基準可能已經退化，先對不變式與最後一個已知良好版本（2026-09-16）

`jsonfmt_20260916`：回寫前「量原檔序列化格式」不能把工作樹現況當成格式真值，因為現況可能已經違反單行／縮排不變式；格式基準應同時取不變式本身與最後一個已知良好版本（例如上次發佈 commit），逐檔量測 `separators`、`ensure_ascii`、結尾換行與是否多行。四組候選無唯一逐位元組命中就停手，多行基準也只能報告不能套用單行規則。

這次 9 份 bank／explanations 由 `e58f0926` 基準還原，2 份基準本來就是多行的 `essays.json` 保持不動；因此「沿用現況格式」不是保守，而可能把退化格式繼續往後傳。
