# 打包、網站化、部署與體積永續 — 教訓日誌

方法正本在 `X.md`。本檔收錄踩坑紀錄，節號沿用 X.md 原編號、永不重編，所以舊引用只要把檔名改成 `X-lessons.md` 就能解析。
新教訓追加在檔尾；畢業成機器閘後仍留在這裡，只在該節開頭加一行「已升級：<腳本名>」。

## 目錄
- 7. 發佈時清單裡有別人改的檔：推之前要自己驗（2026-08-02）
- 8. `.gitignore` 的 `*.bak*` 擋不到**目錄名**（2026-08-02）
- 9. 發佈閘門本身要有版本控管（2026-08-02）
- 10. 把資料 repo 併進工具 repo：`git subtree` ＋ 發佈只取子樹（2026-08-02）
- 11. 🔴 「另走某某路線」的註解要配一個可查的出口（2026-08-06）
- 12. 🔴 落地頁的數字會嚴重過期，而資料層完全看不出來（2026-08-06）
- 13. 🔴 一條沒有被載入的規則，寫在哪一層都一樣是零（2026-08-06）
- 15. 🔴 驗上線內容時，`raw.githubusercontent.com` 自己也有快取（2026-08-09）
- 16. 🔴 衍生產物沒有呼叫者，就只會在有人記得的那天是新的（2026-08-09）
- 17. 🔴 robots.txt 與 llms.txt 可以各自都對、合起來不可能同時遵守（2026-08-09）
- 18. 逐題靜態頁做不到，但「逐題可被指名」做得到（2026-08-09）
- 19. 🔴 打包單檔時，`</script>` 要由打包器逃逸——原始碼自律擋不住註解（2026-08-14）
- 20. 🔴 內嵌資料的收集器，要照著「讀者怎麼讀」收，不是照著「欄位叫什麼」收（2026-08-14）
- 21. 離線單檔放哪：Workers 靜態資源單檔 25 MiB 是硬牆（2026-08-14）

## 7. 發佈時清單裡有別人改的檔：推之前要自己驗（2026-08-02）

多 session 並行時，發佈腳本送出的是**整個 repo 的 HEAD**，不是「我這輪改的東西」。
實測一次發佈 5 個檔，其中 2 個是另一 session 的批次（7,954 則詳解拿掉開頭套語）。
**按下 `--push` 的人為全部內容負責**，不能只驗自己那份。

### 零 token 五道驗收（單行 JSON 的 `git diff` 永遠是「1 行改 1 行」，看不出內容）

| 驗什麼 | 為什麼 |
| --- | --- |
| **筆數對帳**（發佈前 vs 發佈後，逐檔） | 掉筆是最貴且最無聲的損失 |
| **長度只准往一個方向變** | 拿掉套語就只該變短。實測 min=3 max=5、0 則變長＝改動確實只做了宣稱的事 |
| **開頭殘留標點／連接詞／雙冒號** | 砍字串最常見的傷害是把句子開頭砍成「正解：，……」 |
| **空詳解數** | 砍過頭會整則變空 |
| **原字串殘留數** | 量「這次沒清乾淨多少」——它不是回歸，但要記進工單 |

實測結果：0 殘留標點、0 空詳解、0 變長，13 則開頭變成「與…」但那是
「正解：〈名詞片語〉」的合法寫法（拿掉「題眼是」之後），不是砍壞。
另有 232 則沒清到——**那與線上現況相同，不是本次造成的回歸**，
確認它已在工單裡列管後才推。

> **判準：發佈只要求「不比現況壞」，不要求「全部完美」。**
> 把「沒改到的」與「改壞的」分開算，否則永遠不敢按下發佈。

## 8. `.gitignore` 的 `*.bak*` 擋不到**目錄名**（2026-08-02）

`figures_pre_recrop_bak/` 底下 670 個衍生 PNG 被 track 了一個月。
`*.bak*` 是**檔名**樣式，對目錄無效：

```gitignore
*_bak/
*_bak_*/
data/*/raw/
```

止血：`git rm -r --cached <目錄>`（**工作樹檔案保留**，跑完務必數一次實體檔還在）。
根治是「衍生圖不進 repo，部署時重生」——source 是 PDF ＋ 偵測程式 ＋ 少量手修的
`overrides/`。**別用 Git LFS**（把問題換個地方付錢）。

### 🔴 但別把這件事當成 `.git` 變肥的原因——我就這樣誤判了一次

同一輪我看到 `du -sh .git` = 487M（一個月前 93MB），又看到 670 個 PNG 被 track，
就把兩者接成因果寫進 commit message 與工單。**兩個事實都對，因果是我補的，沒量佔比。**

量完之後：

```
.git 487M 的組成         歷史 blob 佔比（全部 1,111 MB）
  177M  packed            data/social-worker/   185.8 MB / 30 blob
  310M  鬆散物件 3,696 個   data/doctor/figures/  123.2 MB / 419 blob
                          *_bak/ ＋ raw/         10.4 MB ← 只有 1%
```

**真正的大宗是資料檔反覆 commit**：`bank.json`／`explanations.json` 每改一次就是
一份幾 MB 的新快照，30 次就是 180MB。那是大型題庫專案的正常成本，不是缺陷。

### 先跑 `git gc`，再談要不要改寫歷史

`git gc` 把鬆散物件收進 packfile 並做差分壓縮，**不動任何 commit、SHA 全不變、
不需要 force push**。實測三個 repo：

| repo | gc 前 | gc 後 |
| --- | ---: | ---: |
| 開發用（含全部歷史） | 487M | **287M** |
| 發佈用（rsync 產生，**從未 pack 過**） | 351M | **241M** |
| skill | 3.0M | **788K** |

> **判準：`du -sh .git` 這個數字本身會誤導**——它把「還沒整理的暫存」與「永久歷史」
> 加在一起。要判斷 repo 是不是真的臃腫，看 `git count-objects -vH` 的 **`size-pack`**，
> 不是資料夾大小。用 `du` 的數字去下「需要改寫歷史」的結論，等於拿沒對焦的尺量東西。

特別注意 **rsync 產生的發佈用 repo 幾乎一定沒 pack 過**（沒人在那裡跑過會觸發
auto-gc 的日常操作），往往是三個 repo 裡最肥的那個。

**歷史改寫（`filter-repo`／BFG）是最後手段**：所有 commit SHA 全變、必須 force push、
別人手上的 clone 全部作廢。先量 `size-pack`、先 `gc`，剩下的量級若仍不可接受再談。

### 把 `gc` 掛進發佈腳本的收尾（含一個自檢被騙過兩次的實錄）

```bash
GC_LOOSE_MIN=${GC_LOOSE_MIN:-500}          # 低於此數整理的效益不值那幾秒
loose_count() { git -C "$1" count-objects -v | awk '/^count:/{print $2}'; }
maybe_gc() {
  local repo="$1" n; n="$(loose_count "$repo")"
  [ "${n:-0}" -lt "$GC_LOOSE_MIN" ] && { echo "· 鬆散物件 ${n:-0} 個，跳過整理"; return 0; }
  git -C "$repo" gc --quiet 2>/dev/null || { echo "⚠ gc 沒跑成功（不影響已發佈內容）"; return 0; }
}
```

掛在 **push 成功之後**，失敗只警告不擋——已經發佈出去的內容不該因為整理沒做而被判失敗。

**自檢寫了三版才擋得住**，每一版都是在 known-bad 上跑才發現破的：

| known-bad 變體 | 第一版自檢 | 修法 |
| --- | --- | --- |
| `loose_count` 讀錯欄位 | ✅ 抓到 | — |
| **把 `git gc` 換成 `true`** | ❌ **沒抓到** | 別驗「有沒有印出『已整理』」，**驗後置條件**：造出鬆散物件 → 跑 → 數量必須歸零 |
| **預設門檻打成 `999999`**（等於永久關閉） | ❌ **沒抓到** | 自檢裡用 `GC_LOOSE_MIN=0` 覆寫門檻去測執行路徑，就**驗不到預設值本身**——要多一條 range 斷言 |
| 門檻分支恆假 | ✅ 抓到 | — |

兩個可推廣的教訓：

1. **驗訊息不等於驗行為。** 「印出成功」是最容易造假的東西，把被測函式換成 `true` 就過。
   要驗的是**它宣稱造成的狀態改變**。
2. 🔴 **自檢裡為了走到某條路徑而覆寫的參數，就是這個自檢驗不到的盲區。**
   用 `GC_LOOSE_MIN=0` 逼 gc 執行的那一刻，「預設值合不合理」就掉出射程了——
   而預設值多打一個零不會有任何錯誤訊息，只會安靜地永遠不整理。
   **每覆寫一個參數，就要補一條專門驗那個參數的斷言。**

## 9. 發佈閘門本身要有版本控管（2026-08-02）

實測踩到：`publish_platform.sh` 是**唯一擋得住敏感字外洩的閘門**，卻放在兩個
git repo 的**外面**——改壞了沒有回溯點。同一層還有 245 支建置腳本，全部一樣。

這種格局很容易長出來：資料 repo 與發佈 repo 各自納管得好好的，而**驅動它們的工具**
落在中間那層沒人管。判準很簡單：**「改壞了會怎樣」最嚴重的那個檔，往往就是沒被納管的那個。**

### 收進來時用白名單，不要用黑名單

第一版寫黑名單（列出跑批產物的樣式），結果漏掉 `*_ALL.json`／`*_lint.json`／`*_gen.js`，
一次要收 **3,556 檔／55MB**。改成白名單後是 **301 檔／2.6MB**：

```gitignore
*            # 預設全擋
!*/          # 但要能走進子目錄
!*.py
!*.sh
!*.md
_dev/        # 白名單之後再把整個目錄擋掉（順序：後面的贏）
_raw/
.venv*/
```

**失敗方向不對稱**：黑名單漏一種＝**靜默進庫**（產物混進歷史、體積長大、diff 全是噪音）；
白名單漏一種＝少收一個看得見的檔。選會吵的那一邊。

### 三個提交前必驗

1. **假的 `.env` 造出來測 `git check-ignore -v`** ——別靠「目前沒有 .env」。
2. **`git ls-files -s | grep ^160000`** ——巢狀 repo 被當成 gitlink 收進來是常見誤收。
3. **密鑰掃描要能分辨自檢 canary**：發佈腳本自己就含 `AKIAIOSFODNN7EXAMPLE`
   這類假密鑰（那是它 selftest 的 known-bad），掃描器會命中它——**看清楚再放行**。

### 🔴 新增 `.git` 會改變裸 `git` 指令的解析結果

原本那層不是 repo，腳本裡的裸 `git xxx` 會報 "not a git repository"；
現在會**安靜地找到新 repo**。收進來之後要 grep 一次所有裸 git 呼叫逐支確認。
（本輪實測：一支用 `git -C <repo>` 明確指定＝安全；一支用 `git hash-object`＝
只看內容、無 `.gitattributes` 過濾器，新舊結果實測完全相同。**但這是驗出來的，不是推的。**）

### 這個 repo 該不該有 remote

**預設不要。** 建置工具會引用內部方法論名稱、私有 repo 名與絕對路徑——
發佈腳本的敏感字掃描樣式**本身就是那份清單**。本機 repo 已經給了回溯點與 diff，
那是當初要納管的全部理由；推到雲端是另一個決定，要單獨問。

## 10. 把資料 repo 併進工具 repo：`git subtree` ＋ 發佈只取子樹（2026-08-02）

工具與資料分兩個 private repo，代價是**同一輪工作被切成兩個 commit、兩段歷史**——
而這類專案的資料**幾乎都是腳本產的**，工具版本與資料版本本來就該綁在一起。
合併是對的；關鍵在**別因此把「絕不能公開」的東西放進發佈路徑**。

### 一行決定安全性

```bash
git -C "$SRC"  archive HEAD        # 合併前：資料 repo 的整個根
git -C "$ROOT" archive HEAD:_dev   # 合併後：只取這個子樹
```

`git archive` 可以只匯出子樹。改成 `HEAD:<prefix>` 之後，工具**結構性地在發佈路徑之外**
——跟分開放時完全一樣。若忘了改，敏感字掃描會擋下發佈（fail-safe），
**但那是最後防線不是設計**。結構正確 > 規則正確：後者只要誰改錯一行排除清單就失效。

### `git subtree add` 保留原始 SHA，代價是路徑不改寫

```bash
mv _dev _dev.orig                        # 工作目錄挪開，不刪
git remote add dev <url> && git fetch dev
git subtree add --prefix=_dev dev main   # 帶完整歷史併入
diff -rq _dev _dev.orig -x .git          # 逐檔比對，追蹤檔必須零差異
```

- ✅ **原始 commit SHA 保留**：`git show <舊SHA>:data/...` 仍取得到。
- ⚠️ **舊 commit 裡的路徑仍是舊的**：`git log -- _dev/data/x` 看不到併入前的歷史，
  要用舊路徑 `git log -- data/x`。

**這個代價是刻意選的**：用 `filter-repo` 改寫路徑會讓所有 SHA 變動，而專案文件
（進度紀錄、ADR、commit 訊息）引用了大量 SHA——**改寫等於讓全部引用失效**。
兩害相權，寧可記住「舊路徑查舊歷史」這條規則。

### 三個一定會咬人的地方

1. **白名單型 `.gitignore` 會把併進來的資料檔全擋掉**。
   `*` ＋ `!*.py/.sh/.md` 的規則會擋掉 `_dev/data/*.json`。要加 `!_dev/**`，
   讓 `_dev/.gitignore` 自己治理——**git 的規則是越深層的 `.gitignore` 越優先**。
2. **被 gitignore 的本機檔不會跟著搬。** `mv` 整個目錄之後，`subtree add` 只還原
   **committed** 的內容；`.bak` 回溯點、裁切前備份、`_scratch/` 全部留在 `.orig` 裡。
   要 `rsync -a --ignore-existing` 補回去，再 `diff -rq` 複驗一次。
3. **前置檢查會誤擋。** 原本寫 `[ -d "$SRC/.git" ]`（來源 repo 存在嗎）——子目錄
   已經沒有 `.git` 了。改驗真正要取的東西：
   `git rev-parse --verify -q "HEAD:_dev"`。**只檢查目錄存在會放過「目錄在、
   但沒 commit 進 repo」**，那正是發佈會拿到空內容的情形。

### 驗收：唯讀發佈的 diff 必須是空的

這是最強的單一證據——**子樹匯出的內容與線上位元相同，代表整個合併沒動到任何會上線的東西**。
比逐項檢查清單可靠，因為它驗的是最終產物而不是中間步驟。

### 🔴 「唯讀發佈」不是唯讀——它會 rsync 並 `git add`，只是不 commit

`publish_platform.sh`（不加 `--push`）跑完之後，`_platform` 的工作樹**已經是新內容、
而且已進 index**。所以「把待發佈內容跟 `_platform/` 比對」是**恆等式**，永遠零差異，
什麼都驗不到。實測就這樣白跑了一輪比對。

要比的是**線上真正的內容**，也就是 `_platform` 的 `HEAD`：

```python
old = json.loads(subprocess.run(["git","-C","_platform","show",f"HEAD:{rel}"],
                                capture_output=True).stdout.decode())
```

一般化：**任何「先同步再提交」的腳本，都要問清楚它的唯讀模式停在哪一步。**
停在 commit 之前 ≠ 停在寫檔之前。

### 推別人的工作前，量結果的方向性，不要讀對方的規則

合併成單一 repo 之後，一次發佈常夾帶其他 session 的成果（實測 15 檔裡 10 檔是
別人的文案 lint，1,791 則詳解被改）。讀對方的 lint 規則只能知道他們**打算**做什麼。

要驗的是**實際發生了什麼**，而且要挑一個「壞掉時必定為非零」的量：

```python
PROSE = "，。；：？！「」『』（）、…—"   # 中文散文標點，不該被折成半形
# 對每則被改的條目，統計 PROSE 各字元的淨減少；非零就逐一目視
```

實測減少 914 筆，逐一看過全部落在算式內（`（7＋8＋8）`→`(7+8+8)`、`3：2`→`3:2`），
散文標點一個沒動——**同一句裡的 `坡度（％）` 與 `M甲：M乙` 因為旁邊有中文而保留全形**。
這個判定式不恆真（它確實抓出 914 筆），也不恆假（誤折會直接反映在同一個數字上）。

順帶：**發佈前的不變式檢查要能區分「本次引入」與「早就在線上」**。
`git show HEAD:<path>` 拿線上版跑同一支檢查——若缺陷兩邊都有，就不該擋這次發佈
（擋了等於用一個舊缺陷扣住一批已驗證的改善）。

### 🔴 驗前端改動前先殺快取——這站有 service worker

改完 `web/run.js` 後在 preview 驗證，量到的是**舊碼的行為**（表頭渲染成「每列一格、
內容是逗號串」）。原因是 `sw.js` 的 cache 還活著，`?cb=` 查詢字串繞不過它。

```js
const regs = await navigator.serviceWorker.getRegistrations();
await Promise.all(regs.map(r => r.unregister()));
const keys = await caches.keys();                 // 實測 obig-shell-v26 / obig-data-v26
await Promise.all(keys.map(k => caches.delete(k)));
```

清完再導頁。**先驗「載到的真的是新碼」再驗行為**：

```js
(await (await fetch('/web/run.js?cb=' + Date.now())).text()).includes('colSpan += 1')
```

這次的意外收穫是那份快取剛好成了 **known-bad 對照**——同一段驗證程式在舊碼下的
輸出長什麼樣，直接看到了。但那是運氣，不是方法：正常做法是先清快取、
確認載到新碼，需要對照時再自己切回舊行為量一次。

---

## 11. 🔴 「另走某某路線」的註解要配一個可查的出口（2026-08-06）

併庫後查真洞（真洞 ＝ `parse:ok` 且非送分且無詳解，再減去 `data_defect`
與 `lint_failed`），殘差裡有 6 題**從未進入生成**。查下去六題全部帶 `figure`，
而 `mkchunks.py` 的取題條件明寫：

> 有 figure → 跳過。從零生成時模型看不到圖，硬寫等於杜撰；**這批另走視覺路線**。

**排除本身是對的**（沒有圖就寫圖題＝杜撰）。黑洞在於**那條「視覺路線」從未被跑，
也沒有佇列追蹤**。全站掃過共 11 題（兩科），其餘八科為 0——因為它們走 R 線重構
（從既有詳解改寫），不經過那份生成清單。

⇒ **這是靜默順延，不是 bug。** 程式碼註解交代了後續，但沒有佇列、沒有計數、
沒有檢查——它與「忘記做」在觀測上完全等價，而且**更難發現，因為註解讓人以為有人在管**。
本專案已有 `data_defect_queue.json` 這個模式，圖題那條當初沒接上去。

⇒ 一般化：**任何「另外處理」的註解都要配一個可查的出口**（工單檔或計數器）。

### 完成率要問「分母是誰算的」

同一輪裡，生成站報 100%、紅隊報 300/300、修復站報 49/49——**三個都對**，
而那 11 題從頭到尾不在任何一個分母裡。生成端的完成率永遠是 100%，
因為分母是它自己的清單。**只有在下游用另一個分母（bank 的 `parse:ok` 題數）
去減，缺口才會顯形。**

## 12. 🔴 落地頁的數字會嚴重過期，而資料層完全看不出來（2026-08-06）

補完一輪詳解後，資料層每一個不變式都通過（`meta.count` 同步、真洞減少、
`okWithNot4Options=0`），但學生看到的落地頁寫著「其中 **599 題**附有逐題詳解
（涵蓋可作答題的 **6%**）」——實際是 **9,460 題（97%）**，**差 16 倍**。

那些是 `gen_static_pages.py` 產生的**靜態 HTML**，數字寫死在檔案裡，
而且不只顯示層：同一個數字同時出現在 `<meta name="description">`、
`og:description`、`twitter:description` 與 **JSON-LD 結構化資料的 FAQ 答案**裡
——搜尋引擎抓的是那份。

⇒ **併庫後一定要重跑 `gen_static_pages.py`**，並在 preview 上核對落地頁數字。

⚠️ 兩個執行細節：
- 這批頁面產生器要用 **`python3`（3.14）**跑，不是詳解鏈那支 `python3.11`——
  `gen_deep_pages.py` 用了巢狀 f-string 含反斜線（PEP 701），3.11 直接 SyntaxError。
- 落地頁數的是「**可作答且有詳解**」的交集，不是 `explanations.json` 的筆數。
  申論題詳解在 `essays.json`、不在 bank，所以某些科會差幾百則——那不是錯誤。

### 清快取不是一個動作，是一串各自獨立的層

`frontend-engine-rules.md` §5 記過「preview 前先註銷 Service Worker ＋ 清 caches」。
實測**照做之後畫面仍是舊的**——SW 清掉了，**HTTP 快取是另一層**。

⇒ 驗證時最可靠的做法不是「清乾淨再看畫面」，而是
**直接 `fetch(url, {cache:'no-store'})` 比對來源字串**——
那繞過所有層，也不依賴你記得清哪幾層。


## 13. 🔴 一條沒有被載入的規則，寫在哪一層都一樣是零（2026-08-06）

**事故**：整輪 8 小時全在做紅隊、修復站、上線，本 skill **一次都沒被載入**。
專案 `CLAUDE.md` 明寫「解析／詳解／紅隊 → 這個 skill」，skill 也早已掛進
`~/.claude/skills/`、symlink 正常。同一輪另一個 skill（`exam-pdf-asset-extractor`）
卻正常觸發——差別在**它的名字被寫在待辦清單的任務格裡**（「先讀 … skill」），
而本 skill 在待辦清單裡只出現在「教訓已寫進 skill」這種**寫回紀錄**。

三個成因，都不是掛載問題：

1. **description 寫成「它是什麼」，不是「何時用」。** 原本是一串用「→」接起來的流程
   自述；使用者真的會講的字——「修復」「上線」「發佈」「跑批」「建置」——**一個都不在
   裡面**。選 skill 是拿使用者的話對 description 的字做匹配，沒有匹配面就選不到。
2. **適用範圍自己寫窄了。** 「何時用這個 skill」只列建置類情境（把試題變題庫、復刻、
   PDF 轉結構化）。照字面讀，**維運**（紅隊、修復站、上線）根本不在範圍內——
   而那正是系統長大以後 90% 的工作。
3. **專案裡存在更近的操作正本。** 待辦清單、紅隊協定、規格書 §0 都比 skill 更具體、
   更可操作，從 agent 視角把 skill 的邊際價值吃光了。

### 量出來的修法（四支探針，known-good／known-bad 兩側）

改完 description（塞進任務動詞與使用者原話）＋補上維運入口＋加一張**任務路由表**
（動詞 → 讀哪份 reference 的哪一節 → 跑哪道必跑門）之後，用 headless 探針實測：

| 探針 | 結果 |
| --- | --- |
| 「跑紅隊」 | ✅ 第一個工具呼叫就是載入 skill |
| 「修復站修一下 X 的詳解」 | ✅ |
| 「準備上線」 | ✅ |
| 「讀待辦清單，執行下一個待辦」 | ❌ **仍不觸發** |
| 「幫我把首頁按鈕改成藍色」（known-bad） | ✅ 不觸發 |
| 「這個 repo 用什麼授權」（known-bad） | ✅ 不觸發 |

第四支是關鍵：那是**長時自動迴圈每輪的真實開場白**。即使在待辦清單檔頭補了一行紅字
「動工前先載入 skill」，探針證明 agent 讀完那行、接著跑了六個工具，仍然沒載入。

⇒ **散文路由對「有任務動詞的指令」有效，對「泛泛的續跑指令」無效。**
後者只能靠機器閘：在**必經之路**（要改題庫就一定得經過 Edit／Write）掛一個
`PreToolUse` hook，沒載入就擋一次並說明。閘要保守到不會被關掉——路徑窄、
每個 session 只擋一次、自己壞掉時放行。

### 可留下來的兩把尺

- **靜態**：`scripts/skill_trigger_lint.py` —— 檢查 description 有沒有那組任務動詞。
  它的價值全在**會對舊版 FAIL**；`--selftest` 把舊 description 當固定的否證樣本留著，
  不要因為「舊的不用了」就刪掉。
- **動態**：headless 探針。判定看**工具呼叫事件**（有沒有出現載入 skill 的呼叫），
  不看模型嘴上說什麼；known-bad 側要證明它不是恆真。


## 15. 🔴 驗上線內容時，`raw.githubusercontent.com` 自己也有快取（2026-08-09）

本手冊一直有一條「**驗上線要驗內容，不要驗 exit code**」，做法是 `curl` 線上的
`bank.json` 回來數。2026-08-09 推完之後照做，結果連續三次都讀到**舊的數字**
（nursing 10,062/10,070，實際推上去的是 10,070/10,070），連加 `?cb=<timestamp>`
與 `Cache-Control: no-cache` 都一樣。

差點就依這個讀數宣告「推送沒生效」，然後回頭去查一個根本不存在的問題。

**`raw.githubusercontent.com` 是 CDN，它的快取可以撐幾分鐘。** 所以：

> 「驗內容不要驗 status code」是對的，但**內容也可能是舊的**。
> 一個會回你舊值的來源，跟一個不回答問題的 status code，誤導性是同一級的。

**零快取、零 base64 的驗法：比對 blob SHA。**

```bash
git hash-object _dev/data/nursing/bank.json
curl -s "https://api.github.com/repos/<owner>/<repo>/contents/data/nursing/bank.json" \
  | python3 -c "import json,sys;print(json.load(sys.stdin)['sha'])"
```

兩邊相同即證明**線上那份就是本機這份**，逐位元組相同。
Git 的 blob SHA 本來就是內容雜湊，所以這是內容比對不是後設資料比對；
而且 API 回的是 SHA 不是內容，**不會把 base64 塞進工具輸出**（那會觸發過濾器、kill session）。

不同才需要繼續查。相同就結束，不要再去讀 CDN。

### 順帶：`publish_platform.sh` 發佈的是 `HEAD:_dev`，不是工作區

同一輪還踩到另一半：把 13 題升成 `parse:ok` 之後**沒有 commit** 就跑發佈，
於是那 13 題沒有出去。而且**預覽也看不出來**——「將發佈的變更」那段同樣是從 `HEAD`
算的，預覽與實際推送**一致地都少了那 13 題**，兩邊互相印證，看起來完全正常。

> 一致不等於正確。當預覽與產出來自同一個錯誤的來源時，它們會互相背書。

發佈前先 `git status _dev/data`，工作區有未提交的改動就先提交再推。

---

## 16. 🔴 衍生產物沒有呼叫者，就只會在有人記得的那天是新的（2026-08-09）

上一節結尾寫著「發佈前先 `git status _dev/data`」——一條**散文規則**。它要生效得先被
想起來。同一份文件裡另外幾條散文規則的下場已經寫在 `gates.md`：重犯即畢業成機器閘。
這一條輪到它畢業了，因為它的失效模式更難看見。

**現場**：全站 1,034 頁靜態頁、`sitemap.xml`、`llms.txt`、`datapackage.json` 全部由
一支生成器從 `bank.json` ＋ manifest 推導出來。稽核那天用 `rg` 反查誰呼叫它——
**沒有任何東西呼叫它**。它只在某個 session 剛好想起來的時候被手跑一次。

新增一卷、補一批詳解，深層頁與索引就落後，而**落後的版本長得跟最新的一模一樣**：
一樣是合法 HTML、一樣 200、一樣有 canonical 與結構化資料。沒有任何一面會紅。
再加上發佈取的是 `HEAD:_dev`，落後的那份就是會上線的那份。

**修法：在唯一必經的出口無條件重跑，重跑後有差異就擋下。**

```zsh
seo_fresh_gate() {
  python3 "$ROOT/_build_features/gen_static_pages.py" >/dev/null || return 1
  dirty="$(git -C "$ROOT" status --porcelain -- _dev/exam _dev/sitemap.xml …)"
  [ -z "$dirty" ] && return 0
  echo "✗ 靜態頁落後於題庫，已就地重新生成；先 commit 再發佈" >&2
  return 1                      # 🔴 擋下，不代使用者 commit——發佈取 HEAD:_dev，
}                               #    腳本替人 commit 是越權，把落差變得看得見才是職責
```

三個決定各有理由，照抄前先確認你的情況一樣：

- **無條件重跑，不做「輸入變了沒」的判斷。** 生成器是純函數（同輸入→同輸出），
  全站 2.4 秒。快取判斷是一個**會寫錯的宣告**，重跑是一個**不會錯的推導**——
  同 IDR-0029。指紋檔會腐爛，重跑不會。
- **擋下而不是自動 commit。** 發佈取的是 `HEAD:_dev`，工作樹的新版帶不上去；
  腳本替使用者 commit 是越權。閘門的職責是把落差變得看得見，不是替人做決定。
- **不掛 hook。** 判準見 `gates.md` 的 hook admission：有一個更晚的、單一必經的
  入口（發佈）能收斂同一件事時，就不要在每次寫檔時攔一刀——那只會打斷連續工作。

**可搬走的判準**：問「這份產物的**上一個**改動是誰觸發的？」答不出一個機器角色，
它就不是被維護的，是被記得的。而記得會失效，且失效時沒有訊號。

---

## 17. 🔴 robots.txt 與 llms.txt 可以各自都對、合起來不可能同時遵守（2026-08-09）

`llms.txt` 與 `datapackage.json` 都寫著「題庫在 `/data/<考試>/bank.json`，請自取」。
`robots.txt` 同時寫著 `Disallow: /data/`。

兩份檔案分開看都合理。合起來，一個守規矩的 agent **只能違背其中一份**，
而它選哪一邊我們看不到——它不會回報，我們的日誌也不會有那一筆。

真正想講的其實是「**可以抓、不要收錄**」（避免幾 MB 的 JSON 佔掉檢索預算、
又被當成網頁收進索引）。那是 `X-Robots-Tag: noindex` 的語意，**不是 `Disallow` 的**：

| 指令 | 意思 | 用在題庫 JSON 上會怎樣 |
| --- | --- | --- |
| `Disallow: /data/` | 不准**抓** | agent 依 llms.txt 來取，卻被自己的規矩擋在門外 |
| `X-Robots-Tag: noindex` | 可以抓，**不要收錄** | 正是想要的：機器取用照舊，不佔索引 |

修法是把 `Disallow` 拿掉，改在 `_headers` 的 `/data/*` 那格加 `X-Robots-Tag: noindex`
（Cloudflare Workers Assets 也吃 `_headers`，實測 `curl -I` 可見）。

斷言怎麼被自己的註解騙過、怎麼修，正本在 gates.md §F′ 推論五；這裡只留產品層結論。

---

## 18. 逐題靜態頁做不到，但「逐題可被指名」做得到（2026-08-09）

阿摩式的「一題一頁」在這個站上被**檔案數上限**否決：全站 46,544 題，
Cloudflare Workers Assets 單次部署的檔案數上限是 20,000，現況全站 3,491 個檔。
差一個數量級，不是調參數能過的。

但拆開來看，「一題一頁」其實同時在買兩樣東西：

1. **題目全文出現在不執行 JS 的 HTML body 裡** —— 這個**一卷一頁已經做到了**。
   貼題幹搜尋要的就是這個，跟一題一頁不一頁無關。
2. **單題有一個可貼、可被引用的網址** —— 這個不必靠檔案，`#q<題號>` 錨點就夠。

所以逐卷頁每題掛一個 `id="q12"` ＋ 一個指向自己的永久連結，
再在 `llms.txt` 明說「**沒有一題一頁的網址，單題請引用錨點**」——
不寫這句，agent 會去猜一個不存在的路徑，然後把 404 當成站壞了。

一個坑：**科目頁的示例題不可以掛同一套錨點**。那幾題抽自不同卷，題號一定會撞，
掛上去就是同一頁出現兩個 `id="q1"`。渲染函式收一個 `anchor=` 參數，
逐卷頁 `True`、示例題 `False`，selftest 兩側各驗一次。

**可搬走的判準**：被資源上限否決的功能，先問它**同時在買幾樣東西**。
通常其中一樣早就有了，另一樣有便宜十倍的做法。

---

## 19. 🔴 打包單檔時，`</script>` 要由打包器逃逸——原始碼自律擋不住註解（2026-08-14）

把 `web/*.js` 內嵌成 `<script>…</script>` 時，**HTML parser 不懂 JS**：它不看字串、
不看註解，只要遇到字面 `</script` 就把標籤關掉。後面的程式碼變成 HTML 文字，
那支檔案等於整個消失。

實測炸掉兩支 —— 而諷刺的是，它們的**程式碼早就防過了**：

```js
/* 用字串拼接組出 script 標籤，避免原始碼出現字面 </script> 影響內嵌打包。 */
var _TAG_OPEN = '<scr' + 'ipt>';        // ← 程式碼很小心
```

出事的是**那句解釋它的註解本身**。另一支則死在檔頭的用法說明
（`<script src="srs.js"></script>`）。兩處都是散文，不是程式碼。

**症狀最壞的地方是它不像壞了**：題庫照樣載入、畫面照樣長出來、題目照樣能作答——
因為死掉的是 SRS 與「含進度副本」這類邊角模組。只有 console 有兩行
`Uncaught SyntaxError: Invalid or unexpected token`。不看 console 就交件了。

修法是**把逃逸放進打包器**，不要放進原始碼規範：

```python
CLOSE_SCRIPT_RE = re.compile(r"</(?=script)", re.IGNORECASE)
code = CLOSE_SCRIPT_RE.sub(r"<\\/", src)     # '<\/script' 在 JS 裡與 '</script' 同值
```

在 JS 字串裡 `'<\/script'` 與 `'</script'` 是**同一個字串值**，在註解裡則純屬無害文字，
所以這個代換不改變任何語意——這正是它可以無差別套用的理由。

**為什麼不能靠原始碼自律**：自律要求每個寫註解的人都記得這條規則，而註解正是
大家最放鬆的地方。打包器只有一支，規則寫一次就永遠生效。**能在收斂點做的檢查，
不要下放給發散點**。

### 配一把尺，因為這個失敗是靜默的

打包器發了幾段 `<script>`，輸出裡就該有幾個 `</script>`：

```python
closers = out.count("</script>")
if closers != emitted:      # emitted = 打包器自己數的段數
    raise SystemExit(f"腳本標籤不平衡：發出 {emitted} 段、輸出 {closers} 個結束標籤")
```

逃逸做對了，輸出裡的 `</script>` 就只剩自己發的那些；多一個＝有人溜過去了。
反方向不必管：`<script>` 出現在字串或註解裡是無害的，parser 只認結束標籤。
（`</style` 同理，但 CSS 沒有 `<\/` 這種逃逸法，只能當場報錯要人改寫。）

---

## 20. 🔴 內嵌資料的收集器，要照著「讀者怎麼讀」收，不是照著「欄位叫什麼」收（2026-08-14）

單檔版要把圖 base64 進 `window.__FIGS__`，得先知道「有哪些圖會被用到」。
第一版寫成：掃每題的 `q.figure` 字串。看起來很合理，實測**靜默漏 35 張**。

因為前端實際上從**三個形狀**取圖名：

| 形狀 | 出處 | 為什麼會有 |
| --- | --- | --- |
| `q.figure` 字串 | 一般圖式題 | 最常見 |
| `q.figure` **陣列** | 題組「甲乙各一張卡牌圖」 | 一題多圖 |
| `q.passage[]` 裡的 `{img:'檔名'}` | 原卷題組「文字→圖→文字→圖」交錯 | 圖要夾在文字中間，不能全丟到最後 |

而 bank 裡另外還有 `needs_figure`、`fig_crop` 兩個欄位——**前端一個都沒讀**，
是建置期的中間產物。照欄位名收，會同時收錯的、漏對的。

**漏圖為什麼看不出來**：前端對載不到的圖有優雅退化，會渲染成「（此題附圖整備中）」。
那句話跟「這題本來就沒圖」在畫面上**一模一樣**。沒有錯誤、沒有破圖 icon、
console 也乾淨（`<img>` 的 404 在 `file://` 下連 log 都不留）。

**判準**：寫「要收哪些東西」的程式前，先去讀**消費端**（這裡是 `run.js` 的
`figureImg()` 兩個呼叫點），把它取值的每條路徑抄成收集器的每條分支。
欄位清單是資料的形狀，讀者才是需求的形狀，兩者會漂移。

> 這是 `frontend-engine-rules.md`「寫進沒有讀者的欄位，等於沒寫」的鏡像：
> 那則講**寫者找不到讀者**，這則講**收集器沒跟上讀者**。同一個病根——
> 資料層自己看不出對錯，只有把兩端擺在一起看才判得出來。

### 順帶：注入了全域變數，就要在同一輪補上讀者

這輪注入 `window.__FIGS__` 時才發現，前端**從來沒有讀過它**（`loader.js` 讀
`__BANK__`、`explain.js` 讀 `__EXPL__`、`essays.js` 讀 `__ESSAYS__`，就是沒人讀 `__FIGS__`）。
打包器產出的是一份「圖全在裡面、但沒有人去拿」的單檔。一行修好：

```js
var src = (window.__FIGS__ && window.__FIGS__[name]) || dataUrl('figures/' + name);
```

三條路徑各驗一次（線上無此變數 → 檔案路徑；有且命中 → data URI；有但缺該檔 →
退回檔案路徑），比只驗「單機版看得到圖」多擋一個線上迴歸。

---

## 21. 離線單檔放哪：Workers 靜態資源單檔 25 MiB 是硬牆（2026-08-14）

單檔版隨科目長大（`json + 圖 × 1.37 + 外殼`），12 科實測 4 MB ～ 170 MB。
如果打算讓它跟網站一起走靜態資源，先量這道牆：

| 限制 | Workers Free | Workers Paid |
| --- | --- | --- |
| **單檔大小** | **25 MiB** | **25 MiB**（付費也不放寬） |
| 檔案數 | 20,000 | 100,000 |

12 科裡有 4 科（170 / 50 / 32 / 30 MB）直接過不去，另有一科 24.8 MB 貼著牆。
**這道牆不能靠付費繞過**，只能換載體。

所以 §4「離線單檔走 GitHub Release」不只是為了 git 不長胖，也是**唯一過得了 25 MiB 的路**
（Release 單檔上限 2 GB）。附帶兩個實務點：

- **資產檔名用純 ASCII**：GitHub 會把 Release 資產名裡非 `[A-Za-z0-9._-]` 的字元換掉，
  中文檔名上去會變成一串點，`releases/latest/download/<檔名>` 這條固定連結也就對不上。
  取 `obig-<考試代碼>.html`；使用者看到的中文名活在**頁面文案與 HTML 的 `<title>`**（分頁標題）裡，
  不必靠檔名。
- **下載頁的數字由 build 當場算**：題數、詳解數、檔案大小全部從這次產出的檔案讀，
  不手寫進 HTML——理由同「落地頁的數字會嚴重過期」那則。只建了部分科目時**不重寫下載頁**，
  否則沒建的科目會被從頁面上抹掉。

### 一個小的排版坑：中文之間不要換行

生成 HTML 時把中文段落折行（原始碼好看），瀏覽器會把那個換行**塌成一個半形空格**，
畫面上就是「留一份在自己 電腦裡」這種憑空多出來的縫。CJK 之間沒有詞距，這個空格
特別顯眼。寧可原始碼一行長，也不要在中文字之間斷行；折行只折在標籤邊界。

